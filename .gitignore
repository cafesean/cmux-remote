node_modules/
.env
.env.*
!.env.example
config.json
*.log
.DS_Store
.setup-state

# agent worktrees — never committed
.claude/

# acceptance evidence stays local: real-machine captures (process listings, absolute paths)
# do not belong in a public tree. run.sh scripts still write here; the durable copy is external.
_specs/p6-handoff/evidence/

# implementer scratch + browser-proof artefacts: outside the branch delta, inside `git add -A`'s
# reach. The durable gate is M10's untracked-file enumeration; these are the known-frequent ones.
*.bak
test-results/
playwright-report/
